#if ($category)
<h1>$category.getName() tasks:</h1>

<ul>
  #foreach($task in $category.getTasks())
   <li>$task.getDescription()</li>
  #end
</ul>
#else
 <em><p>Oops! We can't seem to find the Category.</p></em>

<a href="/categories/$category.getId()/tasks/new">Add a new task</a>
<a href="/categories">All categories</a>
